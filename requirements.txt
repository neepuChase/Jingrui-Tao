# 电力负荷分析与预测项目依赖
# 推荐 Python 3.10+
#
# 说明：
# 1) 本文件保留项目当前代码实际使用到的核心依赖；
# 2) torch 默认不区分 CPU/GPU 轮子，请按你的本机环境安装匹配版本；
# 3) 如已单独安装合适的 PyTorch，可直接继续安装其余依赖。

numpy>=1.24,<3.0
pandas>=2.0,<3.0
matplotlib>=3.7,<4.0
seaborn>=0.12,<1.0
scipy>=1.10,<2.0
statsmodels>=0.14,<1.0
PyEMD>=1.0.0,<2.0.0
holidays>=0.40
chinese-calendar>=1.9
torch>=2.2
